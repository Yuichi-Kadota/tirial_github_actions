name: cmd_wf

on:
  push:
    branches:
      - "main"
    paths:
      - "cmd/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v2

      - name: test cmd
        run: echo cmd_wf_test
